[package]
name = "sap_macros"
version = "0.1.0-alpha.1"
edition = "2021"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1.0.56"
quote = "1.0.26"
syn = { version = "2.0.15", default-features = false, features = ["derive", "parsing", "proc-macro"] }
